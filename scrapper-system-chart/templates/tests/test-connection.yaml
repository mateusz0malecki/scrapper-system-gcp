apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scrapper-system-chart.fullname" . }}-test-connection"
  labels:
    {{- include "scrapper-system-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scrapper-system-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
