apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scrapper-system-chart.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['ping -c 1']
      args: ['{{ .Values.apps.pracuj.app1.name }}:{{ .Values.service.pracujScrapper.port }}']
  restartPolicy: Never
